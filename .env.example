PORT=3000
HOST=0.0.0.0
ORIGIN=http://localhost:3000
DATABASE_PATH=./data/no-name.sqlite
# Set NODE_ENV=production after building. HTTPS reverse proxy required in production.
# TRUST_PROXY=1 only behind a trusted single reverse proxy.
